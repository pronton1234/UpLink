#!/usr/bin/env bash
# Blocks a push whose kit tests fail. Install with:
#   git config core.hooksPath .githooks
set -euo pipefail
echo "pre-push: running UpLinkKit tests…"
"$(git rev-parse --show-toplevel)/scripts/test.sh"
