<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>AppIdentifierPrefix</key>
	<string>$(AppIdentifierPrefix)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.networkextension.packet-tunnel</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).PacketTunnelProvider</string>
	</dict>
	<!-- REQUIRED, and its absence is silent. Since iOS 14 a process that
	     listens on or reaches the local network is blocked without this key —
	     the socket binds, the peer's connection is refused by the system, and
	     nothing anywhere reports why.
	     Observed on hardware 2026-08-20: the Mac dialled 192.168.2.2:50505 and
	     got "no connection within 12s" every twelve seconds against a listener
	     that was running perfectly. -->
	<key>NSLocalNetworkUsageDescription</key>
	<string>UpLink connects to your Mac over the network your Mac hosts. Nothing is sent to any server.</string>
	<!-- The phone advertises this so the Mac can find it; Bonjour is blocked
	     unless the service is declared here by name. -->
	<key>NSBonjourServices</key>
	<array>
		<string>_uplink._tcp</string>
	</array>
</dict>
</plist>
